import React from 'react';
import PropTypes from 'prop-types';

const Popup = props => {
    return (
        <div>
            1. You need to follow your Playlists
            2. Login with Spotify
            3. Select Playlist
            4. Save the mix!
        </div>
    );
};

Popup.propTypes = {};

export default Popup;
